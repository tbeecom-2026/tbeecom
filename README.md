# TBEECOM

Application TBEECOM — transmission de fonds de commerce (site public + CRM interne).

Stack : React + Vite + TypeScript + Tailwind + shadcn/ui, base Neon Postgres.
Hébergement & déploiement : Cloudflare Pages (build `npm run build`, fonctions dans `functions/`).
